output sdl Set Title _("pico-Ce: Across")

var e: Event_ = KeyDown 0
input sdl Event_Timeout (KEYDOWN,&e,2000)

var i: Int = -25
loop {
    output sdl Draw Pixel (i,i)
    input sdl Delay 100
    if bool _(i == 25) {
        break
    }
    set i = _(i + 1)
}

input sdl Event (KEYDOWN,&e)
